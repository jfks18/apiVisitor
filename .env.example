# Copy this file to `.env` and fill in the values.
# Do NOT commit your real `.env` to git.

# --- Server ---
PORT=5001

# --- MySQL ---
# DB_HOST=localhost
# DB_USER=root
# DB_PASS=your_db_password
# DB_NAME=your_db_name

# --- SMTP (Gmail App Password recommended) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your.gmail.address@gmail.com
# Paste your 16-character Gmail App Password here WITHOUT SPACES
SMTP_PASS=olpq hqhm tswl dugk
# Optional "From" header; falls back to SMTP_USER if omitted
SMTP_FROM="Visitor Monitoring" <your.gmail.address@gmail.com>
